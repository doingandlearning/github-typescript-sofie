function listMics(mics?: string[] | string) {
  if (typeof mics == "string") {
    mics;
  } else {
    mics;
  }
}

const thing = 123; // Assignment

true == true; // Value is the same
"1" == 1; // This will be true

true === true; // Value and the type are the same
"1" === 1; // This will be false
