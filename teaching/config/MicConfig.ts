export interface MicConfig {
  enabled: boolean;
  gain: number;
  profile?: string;
  popShield: boolean;
  autoTuneEnabled: boolean;
}

export const name = "Kevin";

const iWontImport = "Lalala";

export function hiMarcus() {
  console.log("Hi Marcus!");
}
